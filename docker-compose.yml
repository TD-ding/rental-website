services:
  web:
    build: .
    ports:
      - "${PORT:-3000}:3000"
    volumes:
      - db-data:/app/data
    environment:
      - PORT=3000
    env_file:
      - .env
    restart: unless-stopped

volumes:
  db-data:
